---
name: kaspa-krc20
description: Deep dive into KRC-20 token standard on Kaspa. Covers architecture, mint/deploy/transfer operations, comparison with BRC-20, and ecosystem tools. Research notes.
author: Nami 🌊
---

# 🪙 Kaspa KRC-20 深度研究

> **狀態**: 研究筆記

---

## 📋 目錄

1. [什麼是 KRC-20？](#1-什麼是-krc-20)
2. [核心原理](#2-核心原理)
3. [與 BRC-20 的關係](#3-與-brc-20-的關係)
4. [技術架構](#4-技術架構)
5. [操作類型](#5-操作類型)
6. [Indexer 機制](#6-indexer-機制)
7. [Kasplex 的 RTD-Rollup](#7-kasplex-的-rtd-rollup)
8. [優缺點分析](#8-優缺點分析)
9. [與其他標準比較](#9-與其他標準比較)
10. [實作範例](#10-實作範例)

---

## 1. 什麼是 KRC-20？

**KRC-20** 是 Kaspa 生態系統上的代幣標準，靈感來自 Bitcoin 的 BRC-20。

### 核心概念

```
KRC-20 = Kaspa + 銘文 (Inscription) + 鏈下索引
```

由於 Kaspa **沒有原生智能合約**，KRC-20 採用「銘文」模式：
- 代幣資料寫入交易的 `OP_RETURN` 或 script
- 鏈下 Indexer 解析交易，維護餘額狀態
- 類似於 Bitcoin Ordinals / BRC-20 的做法

### 主要實現

- **Kasplex** - 最主要的 KRC-20 實現
- 提供 RTD-Rollup 架構作為 L2 增強

---

## 2. 核心原理

### 2.1 銘文 (Inscription) 模式

代幣操作透過在 Kaspa 交易中嵌入 JSON 資料來實現：

```json
{
  "p": "krc-20",      // 協議識別符
  "op": "deploy",     // 操作類型
  "tick": "NACHO",    // 代幣符號 (通常 4 字元)
  "max": "21000000",  // 最大供應量
  "lim": "1000",      // 每次 mint 限額
  "dec": "8"          // 小數位數 (預設 18)
}
```

### 2.2 狀態計算

**鏈上只存資料，不執行邏輯！**

```
餘額狀態 = Σ(所有 mint) - Σ(所有有效 transfer)
```

這個計算由 Indexer 在鏈下完成。

### 2.3 資料流

```
┌─────────────────────────────────────────────────────────────┐
│                      使用者操作                               │
└─────────────────────┬───────────────────────────────────────┘
                      │ 發送交易 (含 JSON 銘文)
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    Kaspa L1 區塊鏈                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ TX: { "p":"krc-20", "op":"mint", "tick":"NACHO" }   │   │
│  └─────────────────────────────────────────────────────┘   │
│  * 只儲存資料，不驗證代幣邏輯                                  │
└─────────────────────┬───────────────────────────────────────┘
                      │ Indexer 掃描區塊
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    KRC-20 Indexer                           │
│  - 解析所有 KRC-20 銘文                                       │
│  - 驗證操作有效性                                             │
│  - 計算餘額狀態                                               │
│  - 維護代幣列表                                               │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    狀態資料庫                                 │
│  - 代幣資訊 (tick, max, lim, dec)                            │
│  - 地址餘額 (address -> balance)                             │
│  - 交易歷史                                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 與 BRC-20 的關係

KRC-20 直接借鑑了 Bitcoin BRC-20 的設計：

| 特性 | BRC-20 (Bitcoin) | KRC-20 (Kaspa) |
|------|------------------|----------------|
| 協議標識 | `"p": "brc-20"` | `"p": "krc-20"` |
| 銘文載體 | Ordinals / Taproot | OP_RETURN / Script |
| 區塊速度 | ~10 分鐘 | ~1 秒 (10 BPS) |
| 確認時間 | 慢 | 極快 |
| Indexer | 多個競爭實現 | Kasplex 為主 |

### BRC-20 原始設計 (by @domodata)

```
"This is just a fun experimental standard demonstrating that 
you can create off-chain balance states with inscriptions."
```

**重要警告**: 這是實驗性標準，設計者明確表示這可能不是最佳方案！

---

## 4. 技術架構

### 4.1 Kaspa 交易結構

```
Kaspa Transaction
├── Inputs
├── Outputs
│   └── OP_RETURN / Script Data
│       └── KRC-20 JSON Inscription
└── ...
```

### 4.2 銘文格式

所有操作都使用 JSON 格式，必須包含：
- `p` (protocol) - 必須是 `"krc-20"`
- `op` (operation) - 操作類型
- `tick` (ticker) - 代幣符號

### 4.3 代幣符號規則

- 通常 4 個字元
- 不區分大小寫 (`NACHO` = `nacho`)
- 先到先得，第一個 deploy 擁有該 ticker

---

## 5. 操作類型

### 5.1 Deploy (部署)

創建新的 KRC-20 代幣：

```json
{
  "p": "krc-20",
  "op": "deploy",
  "tick": "NACHO",
  "max": "21000000",
  "lim": "1000",
  "dec": "8"
}
```

| 欄位 | 必須 | 說明 |
|------|------|------|
| p | ✅ | 協議識別符 |
| op | ✅ | 操作類型：deploy |
| tick | ✅ | 代幣符號 |
| max | ✅ | 最大供應量 |
| lim | ❌ | 每次 mint 限額 |
| dec | ❌ | 小數位數，預設 18 |

### 5.2 Mint (鑄造)

鑄造代幣到發送者地址：

```json
{
  "p": "krc-20",
  "op": "mint",
  "tick": "NACHO",
  "amt": "1000"
}
```

**規則**:
- `amt` 不能超過 `lim`
- 總供應量不能超過 `max`
- 先到先得 (first is first)
- 超過 max 的部分只獲得剩餘額度

### 5.3 Transfer (轉帳)

轉移代幣（兩步驟）：

**步驟 1**: 銘刻 transfer 到自己的地址
```json
{
  "p": "krc-20",
  "op": "transfer",
  "tick": "NACHO",
  "amt": "100"
}
```

**步驟 2**: 將該銘文轉移到目標地址

```
發送者 ---(transfer inscription)---> 接收者
```

**重要規則**:
- 必須有足夠的 Available Balance
- 每個 transfer inscription 只能使用一次
- 發送給自己 = 取消轉帳，恢復餘額

### 5.4 餘額類型

```
Overall Balance    = 總餘額
Transferable Balance = 已銘刻的 transfer 金額
Available Balance  = Overall - Transferable (可用於新銘刻)
```

---

## 6. Indexer 機制

### 6.1 為什麼需要 Indexer？

Kaspa L1 不執行代幣邏輯，只儲存資料。需要 Indexer 來：

1. 掃描所有區塊中的 KRC-20 銘文
2. 驗證操作有效性
3. 計算和維護餘額狀態
4. 提供查詢 API

### 6.2 Indexer 驗證規則

```python
def validate_mint(inscription, state):
    # 1. 檢查代幣是否已 deploy
    if inscription.tick not in state.tokens:
        return False
    
    token = state.tokens[inscription.tick]
    
    # 2. 檢查是否超過 mint limit
    if inscription.amt > token.lim:
        return False
    
    # 3. 檢查總供應量
    if state.circulating[inscription.tick] + inscription.amt > token.max:
        # 只給剩餘額度
        inscription.amt = token.max - state.circulating[inscription.tick]
    
    return inscription.amt > 0

def validate_transfer(inscription, sender, state):
    # 檢查可用餘額
    available = state.balance[sender] - state.transferable[sender]
    return inscription.amt <= available
```

### 6.3 區塊內優先順序

同一區塊內的多筆操作，按確認順序處理：

```
Block N
├── TX 1: mint NACHO 1000  ← 先處理
├── TX 2: mint NACHO 1000  ← 後處理
└── TX 3: transfer NACHO 500
```

---

## 7. Kasplex 的 RTD-Rollup

Kasplex 在基本 KRC-20 之上增加了 **RTD-Rollup** 架構：

### 7.1 什麼是 RTD-Rollup？

**R**eal-**T**ime **D**ata Rollup

```
┌─────────────────────────────────────────┐
│           KRC-20 基礎層                  │
│  (銘文 + Indexer)                        │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│           RTD-Rollup 層                  │
│  - 批量處理交易                           │
│  - 更高效的資料壓縮                        │
│  - 類似 L2 Rollup                        │
└─────────────────────────────────────────┘
```

### 7.2 Kasplex 特性

- **Kaspa L1-Based**: 利用 Kaspa 的高 TPS
- **RTD-Rollup**: 批量打包，提升效率
- **Simple Contract Engine**: 輕量級執行環境
- **Atomic Swap & AMM**: 資產交換協議
- **MEV Resistance**: 抗 MEV 攻擊

---

## 8. 優缺點分析

### ✅ 優點

| 優點 | 說明 |
|------|------|
| **簡單** | 不需要智能合約，JSON 銘文即可 |
| **快速** | 利用 Kaspa 10 BPS，秒級確認 |
| **低成本** | 只需要普通交易費 |
| **公平發行** | First is first，沒有預挖 |
| **抗 MEV** | GHOSTDAG 難以 front-run |

### ❌ 缺點

| 缺點 | 說明 |
|------|------|
| **中心化風險** | 依賴 Indexer，需要信任 |
| **非原生** | 不是 Kaspa 協議的一部分 |
| **一致性問題** | 不同 Indexer 可能有不同狀態 |
| **功能有限** | 只有基本的 deploy/mint/transfer |
| **無合約邏輯** | 無法實現複雜 DeFi |

### ⚠️ 風險

1. **Indexer 故障** - 服務中斷會影響所有操作
2. **分叉風險** - 多個 Indexer 可能產生不同狀態
3. **實驗性質** - 原創者明確表示這是實驗

---

## 9. 與其他標準比較

| 特性 | ERC-20 | BRC-20 | KRC-20 |
|------|--------|--------|--------|
| 區塊鏈 | Ethereum | Bitcoin | Kaspa |
| 智能合約 | ✅ 原生 | ❌ | ❌ |
| 驗證方式 | 鏈上共識 | Indexer | Indexer |
| 區塊時間 | ~12 秒 | ~10 分鐘 | ~1 秒 |
| 可組合性 | 高 | 低 | 低 |
| 去中心化 | 高 | 中 | 中 |
| DeFi 支援 | 完整 | 有限 | 有限 |

---

## 10. 實作範例

### 10.1 查看代幣資訊

```bash
# Kasplex API (示例)
curl https://api.kasplex.org/v1/tokens/NACHO
```

### 10.2 Mint 代幣

```javascript
// 構建 KRC-20 mint 銘文
const inscription = {
  p: "krc-20",
  op: "mint", 
  tick: "NACHO",
  amt: "1000"
};

// 將 JSON 放入交易的 OP_RETURN
const tx = buildKaspaTransaction({
  outputs: [
    { script: OP_RETURN(JSON.stringify(inscription)) }
  ]
});
```

### 10.3 Transfer 代幣

```javascript
// 步驟 1: 銘刻 transfer
const transferInscription = {
  p: "krc-20",
  op: "transfer",
  tick: "NACHO", 
  amt: "100"
};

const inscribeTx = buildKaspaTransaction({
  to: myAddress,  // 發送給自己
  script: OP_RETURN(JSON.stringify(transferInscription))
});

// 步驟 2: 發送銘文到接收者
const sendTx = transferInscription(inscribeTx.id, recipientAddress);
```

---

## 📚 參考資料

- [Kasplex 文件](https://docs-kasplex.gitbook.io/krc20/)
- [BRC-20 原始規範](https://domo-2.gitbook.io/brc-20-experiment)
- [Kaspa 官網](https://kaspa.org)

---

## 🤔 待研究

1. Kasplex 的具體 API 和 SDK
2. RTD-Rollup 的技術細節
3. 如何運行自己的 Indexer
4. KRC-20 的 AMM 和 Atomic Swap 實現

---

*Made with 💙 by Nami 🌊*
